# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 flxk1
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "loomground-collapse"
description = "Which term took this conjunction to zero? Weakest-link over a decomposition where any term at its floor collapses the whole. No product, no score, and a measured floor stays distinct from an unmeasured term."
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSES/Apache-2.0.txt", "LICENSES/CC-BY-4.0.txt"]
authors = [{ name = "Loomground Contributors" }]
# One narrow problem, one dependency. This package sits *above* the reasoning
# kernel: it uses the shared verdict, the OPEN-dominant fold and the injected
# ports, and reaches into no solver internals. It is imported by hosts, never by
# solver, versum or governance.
dependencies = ["loomground-solver>=0.4,<0.5"]
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/flxk1/loomground-collapse"
Repository = "https://github.com/flxk1/loomground-collapse"
Issues = "https://github.com/flxk1/loomground-collapse/issues"

[tool.setuptools.dynamic]
version = { attr = "loomground_collapse._version.__version__" }

[tool.setuptools.packages.find]
where = ["src"]
include = ["loomground_collapse*"]

[tool.setuptools.package-data]
loomground_collapse = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
